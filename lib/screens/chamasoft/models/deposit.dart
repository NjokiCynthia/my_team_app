class Deposit {
  String id, type, name, depositor;
  String date, reconciliation, narrative;
  double amount;

  Deposit({this.id, this.type, this.name, this.depositor, this.date, this.reconciliation, this.narrative, this.amount});
}
